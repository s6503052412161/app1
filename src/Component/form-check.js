import React from 'react'

export default function form-check(){

    const switchReq = ['style','size' ]
    const style = ['bold','italic','underline']
    const size = ['small','medium','large','larger']

    let checkedStyle = []
    let checkedSize =''

    const checkbox {
        let checked = event.target.checked
        let value = 
        let idx = checkedSize.indexOf(value)

        if(checked && idx == -1){
            checked
        }
    }
}
